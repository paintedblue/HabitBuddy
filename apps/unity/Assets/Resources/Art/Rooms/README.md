# Room Prefabs

Drop `KidsRoom.prefab` and `Bathroom.prefab` here.

Use one coherent low-poly asset pack where possible. Include anchors named `RoomFocus`, `BathroomFocus`, and `SinkFocus` for stable camera framing.
